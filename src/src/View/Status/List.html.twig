{% include 'Header.html.twig' %}
{% include 'Menu.html.twig' %}
            
<div class="row">
    <div class="col-md-3"></div>
    <div class="col-md-6">
        <div class="text-right">
            <a class="btn btn-success" href="/status/create">New Status</a>
        </div>
        <br />
        <table class="table">
            <thead class="thead-inverse table-striped">
                <tr>
                    <th>#</th>
                    <th>Name</th>
                    <th></th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                {% if statuses %}
                    {% for status in statuses %}
                        <tr>
                            <td>{{ status.id }}</td>
                            <td>{{ status.name }}</td>
                            <td><a href="/status/edit/{{ status.id}}">Edit</a></td>
                            <td><a href="/status/remove/{{ status.id }}">Remove</a></td>
                        </tr>
                    {% endfor %}
                {% else %}
                    <tr>
                        <td colspan="4" class="text-center">-</td>
                    </tr>
                {% endif %}
            </tbody>
            <tfoot class="thead-inverse">
                <tr>
                    <th>#</th>
                    <th>Státusz</th>
                    <th></th>
                    <th></th>
                </tr>
            </tfoot>
        </table>
    </div>
    <div class="col-md-3"></div>
</div>

{% include 'Footer.html.twig' %}